# Runtime dependencies for Raspberry Pi MVP on Raspberry Pi OS Trixie.
#
# This intentionally excludes the heavy/experimental local SER stack
# (torch/transformers/librosa/faster-whisper). Those stay in
# `requirements.txt` until we wire local emotion/STT fallback.

# Core config / API clients
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
anthropic>=0.39,<1.0
httpx>=0.27,<1.0

# Cloud STT
google-cloud-speech>=2.27,<3.0

# RAG
PyYAML>=6.0,<7.0
chromadb>=1.5,<2.0

# Local encrypted state
sqlcipher3>=0.6.2,<1.0

# Audio I/O
sounddevice>=0.5,<1.0
pyaudio>=0.2,<1.0

# Raspberry Pi GPIO / NeoPixel
gpiozero>=2.0,<3.0
RPi.GPIO>=0.7.1; platform_machine == "armv7l" or platform_machine == "aarch64"
adafruit-blinka>=8.48,<9.0; platform_machine == "armv7l" or platform_machine == "aarch64"
adafruit-circuitpython-neopixel>=6.3,<7.0; platform_machine == "armv7l" or platform_machine == "aarch64"

# Test/smoke tools kept light
pytest>=8.3,<10
