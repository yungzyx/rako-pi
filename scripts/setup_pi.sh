#!/usr/bin/env bash
# Script de provisión inicial de la Raspberry Pi 4.
#
# Instala dependencias del sistema, configura GPIO, audio I/O y
# permisos. NO toca credenciales — esas vienen del .env.
#
# Vacío por ahora; se completa cuando arranca la implementación.

set -euo pipefail

echo "TODO: setup de la Pi (apt, audio, GPIO, systemd unit)."
