"""Botones físicos: pánico y emergencia.

Cuando el botón pánico se presiona, dispara `safety.panic()` con bypass
total al LLM. Debe responder con latencia mínima. La rutina es la misma
para el botón en la app (vía `sync/`).
"""
