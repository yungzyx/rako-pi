"""Repositorios (patrón repository).

Una clase por agregado: `TaskRepository`, `InteractionRepository`,
`EmotionalStateRepository`, `AchievementRepository`, `ConfigRepository`.
Operaciones inmutables: cada update devuelve una entidad nueva.
"""
