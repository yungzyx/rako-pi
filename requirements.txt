# Rako — requirements.txt (puntero de compatibilidad)
#
# Decisión de arquitectura (2026-07): "local para lo que protege, cloud
# para lo que encanta". Las APIs cloud (OpenAI/ElevenLabs/Google) son
# primarias para conversación y voz; lo local cubre seguridad, privacidad
# y resiliencia offline (detector de crisis, SQLCipher, frases
# pre-generadas, whisper.cpp como fallback de STT).
#
# El stack pesado que vivía aquí (torch/transformers/librosa para el SER
# local, langchain, sentence-transformers) NO se usa en el código y quedó
# fuera: el SER está diferido hasta tener datos reales de campo (CLAUDE.md
# §2/§4.2). Si ese proyecto se retoma, sus dependencias vuelven en un
# requirements-ser.txt propio — no acá.
#
# - Runtime en la Raspberry Pi:  requirements-pi-lite.txt (este archivo lo incluye)
# - Desarrollo / CI:             requirements-dev.txt
# - STT offline:                 whisper.cpp se compila aparte (binario, sin pip)

-r requirements-pi-lite.txt
